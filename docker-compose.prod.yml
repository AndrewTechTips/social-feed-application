# Production-like stack:  docker compose -f docker-compose.prod.yml up -d
# Pulls the image built by CI. Override the tag with IMAGE in .env if needed.
#
# Two settings in .env decide whether this is production-*like* or production:
# ENVIRONMENT=production turns on HSTS and puts `Secure` on the refresh cookie,
# and COOKIE_SECURE overrides the latter on its own. Both are deliberately left
# to .env rather than pinned here, because this file also gets run on a laptop
# over plain http — where a Secure cookie is dropped by the browser without a
# word and sign-in stops working for no visible reason.
services:
  # One-shot: bring the schema up to date, then exit. `api` waits for it.
  migrate:
    image: ${IMAGE:-andrewsteel/fastapi:latest}
    command: alembic -c backend/alembic.ini upgrade head
    env_file:
      - .env
    environment:
      - DATABASE_HOSTNAME=postgres
    depends_on:
      postgres:
        condition: service_healthy

  api:
    image: ${IMAGE:-andrewsteel/fastapi:latest}
    ports:
      - "80:8000"
    env_file:
      - .env
    environment:
      - DATABASE_HOSTNAME=postgres
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully

  postgres:
    image: postgres:17
    environment:
      - POSTGRES_USER=${DATABASE_USERNAME}
      - POSTGRES_PASSWORD=${DATABASE_PASSWORD}
      - POSTGRES_DB=${DATABASE_NAME}
    volumes:
      - postgres-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USERNAME}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-db:
