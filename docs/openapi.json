{
  "components": {
    "schemas": {
      "Body_login_api_v1_login_post": {
        "properties": {
          "client_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Client Id"
          },
          "client_secret": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "format": "password",
            "title": "Client Secret"
          },
          "grant_type": {
            "anyOf": [
              {
                "pattern": "^password$",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Grant Type"
          },
          "password": {
            "format": "password",
            "title": "Password",
            "type": "string"
          },
          "scope": {
            "default": "",
            "title": "Scope",
            "type": "string"
          },
          "username": {
            "title": "Username",
            "type": "string"
          }
        },
        "required": [
          "username",
          "password"
        ],
        "title": "Body_login_api_v1_login_post",
        "type": "object"
      },
      "CommentCreate": {
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          }
        },
        "required": [
          "content"
        ],
        "title": "CommentCreate",
        "type": "object"
      },
      "CommentOut": {
        "description": "Something said about a post, with whatever was said back.\n\nReplies are not paged. A conversation arrives whole or the page boundary\nwould fall through the middle of one, and a thread on this app is a handful\nof lines rather than a hundred.",
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "post_id": {
            "title": "Post Id",
            "type": "integer"
          },
          "replies": {
            "default": [],
            "items": {
              "$ref": "#/components/schemas/ReplyOut"
            },
            "title": "Replies",
            "type": "array"
          },
          "user": {
            "$ref": "#/components/schemas/UserOut"
          },
          "user_id": {
            "title": "User Id",
            "type": "integer"
          }
        },
        "required": [
          "id",
          "content",
          "created_at",
          "post_id",
          "user_id",
          "user"
        ],
        "title": "CommentOut",
        "type": "object"
      },
      "CommentPage": {
        "description": "A page of comments. Deliberately the same shape as ``PostPage`` — a\nclient that can page through one can page through the other without\nlearning a second set of field names.",
        "properties": {
          "has_next": {
            "title": "Has Next",
            "type": "boolean"
          },
          "has_prev": {
            "title": "Has Prev",
            "type": "boolean"
          },
          "items": {
            "items": {
              "$ref": "#/components/schemas/CommentOut"
            },
            "title": "Items",
            "type": "array"
          },
          "page": {
            "title": "Page",
            "type": "integer"
          },
          "page_size": {
            "title": "Page Size",
            "type": "integer"
          },
          "pages": {
            "title": "Pages",
            "type": "integer"
          },
          "total": {
            "title": "Total",
            "type": "integer"
          },
          "total_replies": {
            "default": 0,
            "title": "Total Replies",
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total",
          "page",
          "page_size",
          "pages",
          "has_next",
          "has_prev"
        ],
        "title": "CommentPage",
        "type": "object"
      },
      "Detail": {
        "description": "FastAPI's error body, written down so it can be named in `responses=`.\n\nEvery non-validation error this API returns is one of these. Declaring it\nis what turns \"401\" in the docs from a bare number into a shape a client\ncan code against.",
        "example": {
          "detail": "Could not validate credentials"
        },
        "properties": {
          "detail": {
            "title": "Detail",
            "type": "string"
          }
        },
        "required": [
          "detail"
        ],
        "title": "Detail",
        "type": "object"
      },
      "Export": {
        "description": "Everything Commons holds that is yours.\n\nYour drafts are in here. They are unpublished, not secret, and an export\nthat quietly skipped the thing you have not finished would be the one\nomission most likely to matter.",
        "properties": {
          "account": {
            "$ref": "#/components/schemas/UserOut"
          },
          "comments": {
            "items": {
              "$ref": "#/components/schemas/ExportedComment"
            },
            "title": "Comments",
            "type": "array"
          },
          "email": {
            "format": "email",
            "title": "Email",
            "type": "string"
          },
          "exported_at": {
            "format": "date-time",
            "title": "Exported At",
            "type": "string"
          },
          "posts": {
            "items": {
              "$ref": "#/components/schemas/ExportedPost"
            },
            "title": "Posts",
            "type": "array"
          }
        },
        "required": [
          "exported_at",
          "account",
          "email",
          "posts",
          "comments"
        ],
        "title": "Export",
        "type": "object"
      },
      "ExportedComment": {
        "description": "One comment of your own, with enough of its post to make sense alone.\n\n``post_title`` is denormalised into this on purpose. A comment exported as\ncontent plus a post id is a line of text and a number nobody can resolve\nonce the file has left the app; the title is what makes the export\nreadable on its own, which is the only reason anybody downloads one.",
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "parent_id": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "post_id": {
            "title": "Post Id",
            "type": "integer"
          },
          "post_title": {
            "title": "Post Title",
            "type": "string"
          }
        },
        "required": [
          "id",
          "content",
          "created_at",
          "post_id",
          "post_title"
        ],
        "title": "ExportedComment",
        "type": "object"
      },
      "ExportedPost": {
        "description": "A post as you wrote it, which is not a post as the feed shows it.\n\nNo ``votes``, ``voted``, ``saved`` or ``excerpt``. Those are computed for\na viewer — how many people agreed, whether *you* did, whether it is on\nyour shelf, which words matched a search you have just run — and every\none of them is a fact about a moment rather than about the writing. In a\nfile they would be a number that was true once, and ``voted: false``\nagainst your own post would be actively confusing.\n\nNo nested ``user`` either: the account is at the top of the document, and\nrepeating it on every row would be the export padding itself out.",
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "published": {
            "default": true,
            "title": "Published",
            "type": "boolean"
          },
          "title": {
            "title": "Title",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          },
          "user_id": {
            "title": "User Id",
            "type": "integer"
          }
        },
        "required": [
          "title",
          "content",
          "id",
          "created_at",
          "updated_at",
          "user_id"
        ],
        "title": "ExportedPost",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "MeOut": {
        "description": "A person, as they see themselves. Same shape plus the email, because on\nthis one endpoint the caller is the only person it belongs to.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "email": {
            "format": "email",
            "title": "Email",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "username": {
            "title": "Username",
            "type": "string"
          }
        },
        "required": [
          "id",
          "username",
          "created_at",
          "email"
        ],
        "title": "MeOut",
        "type": "object"
      },
      "MeUpdate": {
        "description": "What you may change about yourself. One field, on purpose.\n\nNot the email: it is the credential half of an account, and changing one is\na flow with a confirmation link in it rather than a text box. Not the\npassword either, for the same reason. The username is the public half —\nwhat everybody else sees — and the one that is nobody's business but yours.",
        "properties": {
          "username": {
            "title": "Username",
            "type": "string"
          }
        },
        "required": [
          "username"
        ],
        "title": "MeUpdate",
        "type": "object"
      },
      "NotificationKind": {
        "enum": [
          "reply",
          "comment"
        ],
        "title": "NotificationKind",
        "type": "string"
      },
      "NotificationOut": {
        "description": "One line of \"somebody addressed you\".\n\nIt carries the sentence's three parts — who, what kind, and where — plus a\nshort piece of what they actually said. The excerpt is the reason this is\nreadable rather than a list of links: \"bea replied to you\" tells you\nnothing you can act on, and the first few words of the reply tell you\nwhether to bother.",
        "properties": {
          "actor": {
            "$ref": "#/components/schemas/UserOut"
          },
          "comment_id": {
            "title": "Comment Id",
            "type": "integer"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "excerpt": {
            "title": "Excerpt",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "kind": {
            "$ref": "#/components/schemas/NotificationKind"
          },
          "post": {
            "$ref": "#/components/schemas/NotificationPost"
          },
          "read_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Read At"
          }
        },
        "required": [
          "id",
          "kind",
          "created_at",
          "actor",
          "post",
          "excerpt",
          "comment_id"
        ],
        "title": "NotificationOut",
        "type": "object"
      },
      "NotificationPage": {
        "description": "The same envelope as posts and comments, for the same reason: a client\nthat can walk one list can walk them all.\n\n`total` is also how the unread count is asked for — `?unread=true&\npage_size=1` and read the number — which is the trick the \"new posts\" pill\nalready uses against `/posts/`. One endpoint, no second shape to keep in\nstep, and a poll that costs one row.",
        "properties": {
          "has_next": {
            "title": "Has Next",
            "type": "boolean"
          },
          "has_prev": {
            "title": "Has Prev",
            "type": "boolean"
          },
          "items": {
            "items": {
              "$ref": "#/components/schemas/NotificationOut"
            },
            "title": "Items",
            "type": "array"
          },
          "page": {
            "title": "Page",
            "type": "integer"
          },
          "page_size": {
            "title": "Page Size",
            "type": "integer"
          },
          "pages": {
            "title": "Pages",
            "type": "integer"
          },
          "total": {
            "title": "Total",
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total",
          "page",
          "page_size",
          "pages",
          "has_next",
          "has_prev"
        ],
        "title": "NotificationPage",
        "type": "object"
      },
      "NotificationPost": {
        "description": "Where the notification points. Just enough to draw the line and link it —\nnot a whole PostOut, which would drag the body, the vote count and the\nauthor of every post into a list that is about somebody else entirely.",
        "properties": {
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "title": {
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "id",
          "title"
        ],
        "title": "NotificationPost",
        "type": "object"
      },
      "PostCreate": {
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "published": {
            "default": true,
            "title": "Published",
            "type": "boolean"
          },
          "title": {
            "title": "Title",
            "type": "string"
          }
        },
        "required": [
          "title",
          "content"
        ],
        "title": "PostCreate",
        "type": "object"
      },
      "PostOut": {
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "excerpt": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Excerpt"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "published": {
            "default": true,
            "title": "Published",
            "type": "boolean"
          },
          "saved": {
            "default": false,
            "title": "Saved",
            "type": "boolean"
          },
          "title": {
            "title": "Title",
            "type": "string"
          },
          "updated_at": {
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          },
          "user": {
            "$ref": "#/components/schemas/UserOut"
          },
          "user_id": {
            "title": "User Id",
            "type": "integer"
          },
          "voted": {
            "default": false,
            "title": "Voted",
            "type": "boolean"
          },
          "votes": {
            "default": 0,
            "title": "Votes",
            "type": "integer"
          }
        },
        "required": [
          "title",
          "content",
          "id",
          "created_at",
          "updated_at",
          "user_id",
          "user"
        ],
        "title": "PostOut",
        "type": "object"
      },
      "PostPage": {
        "description": "A page of posts plus the metadata a UI needs to render pagination.",
        "properties": {
          "has_next": {
            "title": "Has Next",
            "type": "boolean"
          },
          "has_prev": {
            "title": "Has Prev",
            "type": "boolean"
          },
          "items": {
            "items": {
              "$ref": "#/components/schemas/PostOut"
            },
            "title": "Items",
            "type": "array"
          },
          "page": {
            "title": "Page",
            "type": "integer"
          },
          "page_size": {
            "title": "Page Size",
            "type": "integer"
          },
          "pages": {
            "title": "Pages",
            "type": "integer"
          },
          "total": {
            "title": "Total",
            "type": "integer"
          }
        },
        "required": [
          "items",
          "total",
          "page",
          "page_size",
          "pages",
          "has_next",
          "has_prev"
        ],
        "title": "PostPage",
        "type": "object"
      },
      "PostSort": {
        "description": "How the feed is ordered.\n\n`new` is the default and needs no explanation. The other two are rankings,\nand what makes them rankings rather than tallies is that both decay — see\ndocs/adr/0008-a-ranking-with-two-gravities.md for how the two constants\nwere chosen, which was by measuring rather than by copying Hacker News.",
        "enum": [
          "new",
          "warm",
          "discussed"
        ],
        "title": "PostSort",
        "type": "string"
      },
      "PostUpdate": {
        "description": "Body for PATCH /posts/{id} — every field optional, only sent ones change.",
        "properties": {
          "content": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Content"
          },
          "published": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "title": "Published"
          },
          "title": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Title"
          }
        },
        "title": "PostUpdate",
        "type": "object"
      },
      "ReplyOut": {
        "description": "An answer to a comment.\n\n**Note what it hasn't got: replies of its own.** That is the one-level rule,\nwritten where a reader of the contract will see it rather than buried in a\nhandler — the shape of the response is the specification, and this shape\ncannot express a third level.",
        "properties": {
          "content": {
            "title": "Content",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "parent_id": {
            "title": "Parent Id",
            "type": "integer"
          },
          "post_id": {
            "title": "Post Id",
            "type": "integer"
          },
          "user": {
            "$ref": "#/components/schemas/UserOut"
          },
          "user_id": {
            "title": "User Id",
            "type": "integer"
          }
        },
        "required": [
          "id",
          "content",
          "created_at",
          "post_id",
          "user_id",
          "user",
          "parent_id"
        ],
        "title": "ReplyOut",
        "type": "object"
      },
      "Token": {
        "description": "What `/login` and `/auth/refresh` hand back.\n\nNote what *isn't* here: the refresh token. It goes out in an httpOnly\ncookie, where no script on the page can read it, which is the whole reason\nthere are two tokens rather than one long-lived one.",
        "example": {
          "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjo3fQ.sig",
          "csrf_token": "0lNrK8s2Qm6Xv3dYpHtW1cJfB9aZuE4g",
          "expires_in": 900,
          "token_type": "bearer"
        },
        "properties": {
          "access_token": {
            "description": "Send as `Authorization: Bearer …`. Hold it in memory, not in storage — it expires in minutes and `/auth/refresh` replaces it.",
            "title": "Access Token",
            "type": "string"
          },
          "csrf_token": {
            "description": "Echo this back as an `X-CSRF-Token` header on `/auth/refresh` and `/auth/logout`. It changes on every rotation.",
            "title": "Csrf Token",
            "type": "string"
          },
          "expires_in": {
            "description": "Seconds until the access token expires. Refresh a little before this rather than waiting for a 401.",
            "title": "Expires In",
            "type": "integer"
          },
          "token_type": {
            "default": "bearer",
            "title": "Token Type",
            "type": "string"
          }
        },
        "required": [
          "access_token",
          "expires_in",
          "csrf_token"
        ],
        "title": "Token",
        "type": "object"
      },
      "UserCreate": {
        "properties": {
          "email": {
            "format": "email",
            "title": "Email",
            "type": "string"
          },
          "password": {
            "minLength": 8,
            "title": "Password",
            "type": "string"
          },
          "username": {
            "title": "Username",
            "type": "string"
          }
        },
        "required": [
          "username",
          "email",
          "password"
        ],
        "title": "UserCreate",
        "type": "object"
      },
      "UserOut": {
        "description": "A person, as everybody else sees them. No email: that's a credential,\nand handing it to anyone who can walk a list of ids is how address books\nget scraped.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "id": {
            "title": "Id",
            "type": "integer"
          },
          "username": {
            "title": "Username",
            "type": "string"
          }
        },
        "required": [
          "id",
          "username",
          "created_at"
        ],
        "title": "UserOut",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "Vote": {
        "example": {
          "dir": 1,
          "post_id": 12
        },
        "properties": {
          "dir": {
            "description": "1 to add your upvote, 0 to take it back. There is no downvote and there is not going to be one.",
            "enum": [
              0,
              1
            ],
            "title": "Dir",
            "type": "integer"
          },
          "post_id": {
            "title": "Post Id",
            "type": "integer"
          }
        },
        "required": [
          "post_id",
          "dir"
        ],
        "title": "Vote",
        "type": "object"
      }
    },
    "securitySchemes": {
      "OAuth2PasswordBearer": {
        "flows": {
          "password": {
            "scopes": {},
            "tokenUrl": "api/v1/login"
          }
        },
        "type": "oauth2"
      }
    }
  },
  "info": {
    "contact": {
      "name": "Source on GitHub",
      "url": "https://github.com/AndrewTechTips/social-feed-application"
    },
    "description": "The backend behind **Commons** — a small public feed.\n\nPosts, comments, upvotes and accounts, with a two-token auth flow. Reading is public; writing needs an account.\n\nThe published demo at [andrewtechtips.github.io/social-feed-application](https://andrewtechtips.github.io/social-feed-application/) reimplements this same contract in the browser, because a static host has nowhere to run FastAPI. This document is the contract both of them answer to.",
    "license": {
      "name": "MIT",
      "url": "https://github.com/AndrewTechTips/social-feed-application/blob/main/LICENSE"
    },
    "title": "Social Feed API",
    "version": "1.0.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/v1/auth/logout": {
      "post": {
        "description": "End the session this cookie belongs to, and clear the cookie.\n\nRevokes server-side rather than only clearing the cookie, which is most of\nwhy the sessions are in a table at all: clearing a cookie ends a session on\none machine, revoking it ends the session.",
        "operationId": "logout_api_v1_auth_logout_post",
        "responses": {
          "204": {
            "description": "Signed out. Answered whether or not there was a session to end — a caller asking to leave should never be told no."
          }
        },
        "summary": "Sign out",
        "tags": [
          "Authentication"
        ]
      }
    },
    "/api/v1/auth/logout-all": {
      "post": {
        "description": "End every session this account has, including this one.\n\nAuthenticated with the *access* token rather than the refresh cookie, which\nis the difference between this and `/logout`. Signing out one browser is\nsomething a browser asks for and needs no proof beyond holding the cookie;\nsigning out every browser is something a person asks for, and it should\ntake more than having once been handed a cookie to do it.\n\nThat is also why there is a `refresh_sessions` table at all. A stateless\nrefresh token cannot be taken back, so this would have been a button that\ncleared one cookie and lied about the rest.",
        "operationId": "logout_everywhere_api_v1_auth_logout_all_post",
        "responses": {
          "204": {
            "description": "Every session ended, on every machine."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Sign out everywhere",
        "tags": [
          "Authentication"
        ]
      }
    },
    "/api/v1/auth/refresh": {
      "post": {
        "description": "Rotate the session.\n\nEvery call invalidates the token that was presented and issues the next\none, so a copied cookie is detectable: the copy eventually presents a\nsecret that has already been rotated away, and the whole session is ended\nrather than guessing which of the two holders is the thief.\n\nSend the `csrf_token` from the last `/login` or `/auth/refresh` response in\nan `X-CSRF-Token` header. Without it this answers 401 even with a perfectly\ngood cookie.",
        "operationId": "refresh_api_v1_auth_refresh_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Token"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No usable refresh cookie, or no matching `X-CSRF-Token`. One answer for every way of being wrong — saying which would tell a caller whether they hold half of a real session. The cookie is cleared, because whatever is in the browser is no longer worth sending."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "Too many attempts (30/minute)."
          }
        },
        "summary": "Trade the refresh cookie for a new access token",
        "tags": [
          "Authentication"
        ]
      }
    },
    "/api/v1/comments/{id}": {
      "delete": {
        "description": "Yours to remove, and nobody else's.\n\nNote what this deliberately isn't: the author of the *post* can't delete\ncomments on it. Moderation is a feature with a scope of its own — who may\nremove what, whether the person who wrote it is told, whether it leaves a\ntombstone — and quietly granting it to post authors here would be a policy\ndecision smuggled in as an ownership check.",
        "operationId": "delete_comment_api_v1_comments__id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Removed."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The post exists, is visible to you, and isn't yours. Used only where ownership is the question — never to hide a draft, which answers 404."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Remove your comment",
        "tags": [
          "Comments"
        ]
      }
    },
    "/api/v1/login": {
      "post": {
        "description": "Exchange an email and a password for an access token and a refresh cookie.\n\nThe form field is called `username` because that is what the OAuth2\npassword flow calls it, and `/docs` builds its form from the same spec.\nThis API signs people in by **email** — the username is the public identity\nand is not a credential.",
        "operationId": "login_api_v1_login_post",
        "requestBody": {
          "content": {
            "application/x-www-form-urlencoded": {
              "schema": {
                "$ref": "#/components/schemas/Body_login_api_v1_login_post"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Token"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The email and password don't match. Deliberately the same answer whether or not the address has an account."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "Too many attempts (5/minute)."
          }
        },
        "summary": "Sign in",
        "tags": [
          "Authentication"
        ]
      }
    },
    "/api/v1/notifications/": {
      "get": {
        "description": "Newest first, because a notification is about what just happened.\n\nYours only, and that is not a filter a caller can lift: the `user_id`\npredicate is applied here and there is no parameter that changes it.",
        "operationId": "list_notifications_api_v1_notifications__get",
        "parameters": [
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "default": 1,
              "minimum": 1,
              "title": "Page",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page_size",
            "required": false,
            "schema": {
              "default": 20,
              "maximum": 50,
              "minimum": 1,
              "title": "Page Size",
              "type": "integer"
            }
          },
          {
            "description": "Only the ones you haven't seen. With `page_size=1` this is also how you ask for the unread *count*: the answer is the envelope's `total`.",
            "in": "query",
            "name": "unread",
            "required": false,
            "schema": {
              "default": false,
              "description": "Only the ones you haven't seen. With `page_size=1` this is also how you ask for the unread *count*: the answer is the envelope's `total`.",
              "title": "Unread",
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "has_next": false,
                  "has_prev": false,
                  "items": [
                    {
                      "actor": {
                        "created_at": "2026-08-30T17:45:11.002Z",
                        "id": 9,
                        "username": "tessaward"
                      },
                      "comment_id": 31,
                      "created_at": "2026-09-09T07:14:30.881Z",
                      "excerpt": "I've walked past that wall a hundred times and never once thought about it.",
                      "id": 4,
                      "kind": "reply",
                      "post": {
                        "id": 12,
                        "title": "The library that stays open all night"
                      }
                    }
                  ],
                  "page": 1,
                  "page_size": 10,
                  "pages": 1,
                  "total": 4
                },
                "schema": {
                  "$ref": "#/components/schemas/NotificationPage"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "What has been said to you",
        "tags": [
          "Notifications"
        ]
      }
    },
    "/api/v1/notifications/read": {
      "post": {
        "description": "Mark every unread notification of yours as seen.\n\nAll of them, rather than the ones a client happens to have drawn. The badge\nexists to say \"there is something new\", and a badge that goes on saying so\nafter you have looked is a badge people stop believing.\n\nOne UPDATE, not a read-then-write loop: the rows are not wanted back.",
        "operationId": "mark_all_read_api_v1_notifications_read_post",
        "responses": {
          "204": {
            "description": "Seen. Answered whether or not there was anything."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Mark everything as seen",
        "tags": [
          "Notifications"
        ]
      }
    },
    "/api/v1/posts/": {
      "get": {
        "description": "The feed, and the search over it.\n\nSearching covers the title *and* the body: the old title-only ``LIKE``\nmeant a post about repairing a kettle couldn't be found by the word\n\"kettle\" unless it happened to be in the heading, which is not what anyone\ntyping into a search box expects.",
        "operationId": "get_posts_api_v1_posts__get",
        "parameters": [
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "default": 1,
              "minimum": 1,
              "title": "Page",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page_size",
            "required": false,
            "schema": {
              "default": 10,
              "maximum": 100,
              "minimum": 1,
              "title": "Page Size",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "search",
            "required": false,
            "schema": {
              "default": "",
              "maxLength": 100,
              "title": "Search",
              "type": "string"
            }
          },
          {
            "description": "`new` is newest first. `warm` and `discussed` are rankings that decay with age, so neither becomes a museum of whatever was popular two years ago. Ignored while searching, where relevance leads. See ADR 0008 for the two constants.",
            "in": "query",
            "name": "sort",
            "required": false,
            "schema": {
              "$ref": "#/components/schemas/PostSort",
              "default": "new",
              "description": "`new` is newest first. `warm` and `discussed` are rankings that decay with age, so neither becomes a museum of whatever was popular two years ago. Ignored while searching, where relevance leads. See ADR 0008 for the two constants."
            }
          },
          {
            "description": "Only posts written *after* this moment — the other end of the window `as_of` closes. Ask for one page of one with it and the envelope's `total` is exactly how many posts have arrived since, which is what a \"new posts\" control needs and the whole of what it needs. URL-encode it, for the reason given on `as_of`.",
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "date-time",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only posts written *after* this moment — the other end of the window `as_of` closes. Ask for one page of one with it and the envelope's `total` is exactly how many posts have arrived since, which is what a \"new posts\" control needs and the whole of what it needs. URL-encode it, for the reason given on `as_of`.",
              "title": "Since"
            }
          },
          {
            "description": "Only posts written at or before this moment. Send back the `created_at` of the newest post the first page gave you, and every page after it is served out of the same feed — otherwise a post written while you scroll pushes the rest down and you are handed one you have already read. URL-encode it: a timestamp carries a `+` in its offset, and a bare `+` in a query string decodes to a space.",
            "in": "query",
            "name": "as_of",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "date-time",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only posts written at or before this moment. Send back the `created_at` of the newest post the first page gave you, and every page after it is served out of the same feed — otherwise a post written while you scroll pushes the rest down and you are handed one you have already read. URL-encode it: a timestamp carries a `+` in its offset, and a bare `+` in a query string decodes to a space.",
              "title": "As Of"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "has_next": true,
                  "has_prev": false,
                  "items": [
                    {
                      "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                      "created_at": "2026-09-08T22:41:07.220Z",
                      "id": 12,
                      "published": true,
                      "saved": false,
                      "title": "The library that stays open all night",
                      "updated_at": "2026-09-08T22:41:07.220Z",
                      "user": {
                        "created_at": "2026-08-21T09:14:02.511Z",
                        "id": 7,
                        "username": "marenholt"
                      },
                      "user_id": 7,
                      "voted": false,
                      "votes": 4
                    }
                  ],
                  "page": 1,
                  "page_size": 10,
                  "pages": 2,
                  "total": 14
                },
                "schema": {
                  "$ref": "#/components/schemas/PostPage"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "summary": "The feed",
        "tags": [
          "Posts"
        ]
      },
      "post": {
        "description": "Create a post, attributed to the token's owner.\n\n`published: false` saves it as a draft — which is an access rule, not a\ndisplay hint: it is returned to you and to nobody else.",
        "operationId": "create_posts_api_v1_posts__post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PostCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "example": {
                  "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                  "created_at": "2026-09-08T22:41:07.220Z",
                  "id": 12,
                  "published": true,
                  "saved": false,
                  "title": "The library that stays open all night",
                  "updated_at": "2026-09-08T22:41:07.220Z",
                  "user": {
                    "created_at": "2026-08-21T09:14:02.511Z",
                    "id": 7,
                    "username": "marenholt"
                  },
                  "user_id": 7,
                  "voted": false,
                  "votes": 0
                },
                "schema": {
                  "$ref": "#/components/schemas/PostOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Write a post",
        "tags": [
          "Posts"
        ]
      }
    },
    "/api/v1/posts/{id}": {
      "delete": {
        "description": "Delete a post of yours.\n\nThe comments and votes on it go too, by `ON DELETE CASCADE` at the\ndatabase, so it holds whoever issues the DELETE.",
        "operationId": "delete_post_api_v1_posts__id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Gone, along with its comments and its votes."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The post exists, is visible to you, and isn't yours. Used only where ownership is the question — never to hide a draft, which answers 404."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Delete a post",
        "tags": [
          "Posts"
        ]
      },
      "get": {
        "description": "One post, with its vote count.\n\nPublic, like the feed — a token is optional and only changes whether your\nown drafts are reachable.",
        "operationId": "get_post_api_v1_posts__id__get",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                  "created_at": "2026-09-08T22:41:07.220Z",
                  "id": 12,
                  "published": true,
                  "saved": false,
                  "title": "The library that stays open all night",
                  "updated_at": "2026-09-08T22:41:07.220Z",
                  "user": {
                    "created_at": "2026-08-21T09:14:02.511Z",
                    "id": 7,
                    "username": "marenholt"
                  },
                  "user_id": 7,
                  "voted": false,
                  "votes": 4
                },
                "schema": {
                  "$ref": "#/components/schemas/PostOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "One post",
        "tags": [
          "Posts"
        ]
      },
      "patch": {
        "description": "Change part of a post of yours.\n\nOnly the fields actually present in the body move. An empty body is a 400\nrather than a no-op 200: it almost always means the caller built the\nrequest wrong.",
        "operationId": "patch_post_api_v1_posts__id__patch",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PostUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                  "created_at": "2026-09-08T22:41:07.220Z",
                  "id": 12,
                  "published": true,
                  "saved": false,
                  "title": "The library that stays open all night",
                  "updated_at": "2026-09-08T22:41:07.220Z",
                  "user": {
                    "created_at": "2026-08-21T09:14:02.511Z",
                    "id": 7,
                    "username": "marenholt"
                  },
                  "user_id": 7,
                  "voted": false,
                  "votes": 4
                },
                "schema": {
                  "$ref": "#/components/schemas/PostOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The request was understood and asks for nothing."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The post exists, is visible to you, and isn't yours. Used only where ownership is the question — never to hide a draft, which answers 404."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Change part of a post",
        "tags": [
          "Posts"
        ]
      },
      "put": {
        "description": "Replace a post of yours, in full.\n\nEvery field of the body is applied, so anything you leave out goes back to\nits default. `PATCH` is the one that changes only what you send.",
        "operationId": "update_post_api_v1_posts__id__put",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PostCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                  "created_at": "2026-09-08T22:41:07.220Z",
                  "id": 12,
                  "published": true,
                  "saved": false,
                  "title": "The library that stays open all night",
                  "updated_at": "2026-09-08T22:41:07.220Z",
                  "user": {
                    "created_at": "2026-08-21T09:14:02.511Z",
                    "id": 7,
                    "username": "marenholt"
                  },
                  "user_id": 7,
                  "voted": false,
                  "votes": 4
                },
                "schema": {
                  "$ref": "#/components/schemas/PostOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The post exists, is visible to you, and isn't yours. Used only where ownership is the question — never to hide a draft, which answers 404."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Replace a post",
        "tags": [
          "Posts"
        ]
      }
    },
    "/api/v1/posts/{id}/save": {
      "delete": {
        "description": "Remove a post from your shelf.\n\nNo 404 on this one, deliberately, and not for symmetry: there is nothing\nhere worth reporting as missing. Whether the post exists, whether you could\nsee it, and whether you had saved it all end in the same place — it is not\non your shelf — and three ways of saying that would be three branches a\nclient has to handle to reach one outcome.",
        "operationId": "unsave_post_api_v1_posts__id__save_delete",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Off your shelf — including when it was never on it. The state you asked for is the state you're in."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Take a post off your shelf",
        "tags": [
          "Shelf"
        ]
      },
      "put": {
        "description": "Save a post you can see.\n\nA draft you don't own answers 404, exactly as reading it would — a 403\nwould confirm that it exists.",
        "operationId": "save_post_api_v1_posts__id__save_put",
        "parameters": [
          {
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "title": "Id",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "On your shelf. Saying so again is the same answer — this is a set, and asking for a post to be in it twice is not an error."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Put a post on your shelf",
        "tags": [
          "Shelf"
        ]
      }
    },
    "/api/v1/posts/{post_id}/comments": {
      "get": {
        "description": "One page of a post's conversations, **oldest first**, each with whatever\nwas said back to it.\n\nThe feed runs newest-first because you arrive at it to see what's new. A\ncomment thread is read top to bottom like a conversation, so it runs the\nother way — and the page size is larger, because twenty short lines is\nabout one screen where ten posts is several.\n\n**The page is over top-level comments, and replies come with their\nparent.** Paging the two together would eventually put a page boundary\nthrough the middle of an exchange, which is the one place a boundary must\nnot fall; and a reply can only ever belong to something on the same page as\nitself, so the alternative buys nothing. `total` therefore counts\nconversations — it is what `pages` is computed from — and `total_replies`\nsays how many messages hang off them, so a heading can report the size of\nthe conversation without walking it.",
        "operationId": "get_comments_api_v1_posts__post_id__comments_get",
        "parameters": [
          {
            "in": "path",
            "name": "post_id",
            "required": true,
            "schema": {
              "title": "Post Id",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "default": 1,
              "minimum": 1,
              "title": "Page",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page_size",
            "required": false,
            "schema": {
              "default": 20,
              "maximum": 100,
              "minimum": 1,
              "title": "Page Size",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "has_next": false,
                  "has_prev": false,
                  "items": [
                    {
                      "content": "I've walked past that wall a hundred times and never once thought about it.",
                      "created_at": "2026-09-09T07:02:55.104Z",
                      "id": 31,
                      "post_id": 12,
                      "user": {
                        "created_at": "2026-08-30T17:45:11.002Z",
                        "id": 9,
                        "username": "tessaward"
                      },
                      "user_id": 9
                    }
                  ],
                  "page": 1,
                  "page_size": 10,
                  "pages": 1,
                  "total": 5
                },
                "schema": {
                  "$ref": "#/components/schemas/CommentPage"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "summary": "The conversation on a post",
        "tags": [
          "Comments"
        ]
      },
      "post": {
        "description": "Add a comment to a post you can see, or a reply to one of its comments.\n\nWhitespace is trimmed and an empty comment is a 422 — the cap is enforced\nhere and not only in the form, because this is the field a script would\npoint at first.\n\n`parent_id` makes it a reply, and there are exactly two ways that can be\nrefused. A parent that isn't a comment on *this* post is a 404 rather than\na 400: from here it does not exist, and saying otherwise would confirm the\nexistence of comments on a draft somebody hasn't published. A parent that\nis itself a reply is a 400, because the request is well-formed and simply\nasks for something this app does not do.",
        "operationId": "create_comment_api_v1_posts__post_id__comments_post",
        "parameters": [
          {
            "in": "path",
            "name": "post_id",
            "required": true,
            "schema": {
              "title": "Post Id",
              "type": "integer"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CommentCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "example": {
                  "content": "I've walked past that wall a hundred times and never once thought about it.",
                  "created_at": "2026-09-09T07:02:55.104Z",
                  "id": 31,
                  "post_id": 12,
                  "user": {
                    "created_at": "2026-08-30T17:45:11.002Z",
                    "id": 9,
                    "username": "tessaward"
                  },
                  "user_id": 9
                },
                "schema": {
                  "$ref": "#/components/schemas/CommentOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The request was understood and asks for nothing."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Say something about a post",
        "tags": [
          "Comments"
        ]
      }
    },
    "/api/v1/shelf": {
      "get": {
        "description": "Your shelf, most recently saved first.\n\nOrdered by when you saved it rather than by when it was written, which is\nthe difference between a shelf and a feed: what you put there last is what\nyou meant to read next. `ix_saves_user_id_created_at` hands that ordering\nback without a sort.\n\nIt is one page of the feed's own machinery, so the visibility rule, the\nvote counts and `voted` are all the same here as anywhere else.",
        "operationId": "get_shelf_api_v1_shelf_get",
        "parameters": [
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "default": 1,
              "minimum": 1,
              "title": "Page",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page_size",
            "required": false,
            "schema": {
              "default": 10,
              "maximum": 100,
              "minimum": 1,
              "title": "Page Size",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "has_next": false,
                  "has_prev": false,
                  "items": [
                    {
                      "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                      "created_at": "2026-09-08T22:41:07.220Z",
                      "id": 12,
                      "published": true,
                      "saved": true,
                      "title": "The library that stays open all night",
                      "updated_at": "2026-09-08T22:41:07.220Z",
                      "user": {
                        "created_at": "2026-08-21T09:14:02.511Z",
                        "id": 7,
                        "username": "marenholt"
                      },
                      "user_id": 7,
                      "voted": false,
                      "votes": 4
                    }
                  ],
                  "page": 1,
                  "page_size": 10,
                  "pages": 1,
                  "total": 6
                },
                "schema": {
                  "$ref": "#/components/schemas/PostPage"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "What you saved",
        "tags": [
          "Shelf"
        ]
      }
    },
    "/api/v1/users/": {
      "post": {
        "description": "Register.\n\nThe username is folded to lower case and must match\n`^[a-z][a-z0-9_-]{2,19}$` — starting with a letter, so a username can never\nbe mistaken for an id in a URL. A handful of names the API needs for itself\n(`me`, `admin`, `api`, `root`, `commons`) are refused.\n\nThe response is a `UserOut` and carries no email. Registering does not sign\nyou in: `POST /login` does that.",
        "operationId": "create_user_api_v1_users__post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UserCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "example": {
                  "created_at": "2026-08-21T09:14:02.511Z",
                  "id": 7,
                  "username": "marenholt"
                },
                "schema": {
                  "$ref": "#/components/schemas/UserOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "The username or the email is already registered. The message names which — for a username that's unavoidable, since you can't pick one without knowing it's free."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "Rate limited. The message names the limit you hit."
          }
        },
        "summary": "Create an account",
        "tags": [
          "Users"
        ]
      }
    },
    "/api/v1/users/me": {
      "delete": {
        "description": "Delete your account and everything attached to it.\n\nEverything means everything: posts, the comments on them, the comments you\nleft elsewhere, your votes, and every session you have open. None of that\nis spelled out here — each one is an `ON DELETE CASCADE` declared on the\ncolumn that points at you, so it holds whoever issues the DELETE and cannot\ndrift from what this function happens to remember to clean up.\n\nThe refresh cookie is cleared on the way out. It names a session row that\nno longer exists, so the browser would recover on its own the next time it\ntried; leaving somebody holding a credential for an account that is gone is\nnevertheless not a tidy way to say goodbye.",
        "operationId": "delete_me_api_v1_users_me_delete",
        "responses": {
          "204": {
            "description": "Gone, along with everything you wrote."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Delete your account",
        "tags": [
          "Users"
        ]
      },
      "get": {
        "description": "Who the caller is.\n\nLogging in takes an email and a password and hands back a token, which\ntells the client nothing about the person it just signed in. That was\nsurvivable while posts carried their author's email and the client could\nmatch on it; now that the public identity is a username and the email never\nleaves this endpoint, something has to close the gap. Without it a browser\nwith no local history can sign in successfully and still not know which\nposts are its own.",
        "operationId": "get_me_api_v1_users_me_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "created_at": "2026-08-21T09:14:02.511Z",
                  "email": "maren.holt@example.com",
                  "id": 7,
                  "username": "marenholt"
                },
                "schema": {
                  "$ref": "#/components/schemas/MeOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Who the caller is",
        "tags": [
          "Users"
        ]
      },
      "patch": {
        "description": "Change the name everybody else sees.\n\nOnly the username. The email and the password are the credential half of an\naccount and changing either is a flow with a confirmation in it, not a text\nbox on a settings screen.\n\nA name that is taken is a 409 with a sentence, the same answer registering\ngives — and for the same reason it is caught from the database rather than\nchecked first: two people asking for the same free name in the same moment\nboth pass a lookup and one of them still has to lose.\n\nNothing else moves. Posts, comments and votes are joined by id, and the id\nis the half of an identity that was always meant to be the durable one.",
        "operationId": "update_me_api_v1_users_me_patch",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MeUpdate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "created_at": "2026-08-21T09:14:02.511Z",
                  "email": "maren.holt@example.com",
                  "id": 7,
                  "username": "marenholt"
                },
                "schema": {
                  "$ref": "#/components/schemas/MeOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "That would collide with something already there."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Change your username",
        "tags": [
          "Users"
        ]
      }
    },
    "/api/v1/users/me/export": {
      "get": {
        "description": "Everything you have written here, in one JSON document.\n\n── why this is one endpoint and not two paged ones ────────────────────\nYour posts are already at ``/users/{username}/posts``, and a\n``/users/{username}/comments`` beside it would have been the consistent\nthing to build. Two problems with composing an export out of those.\n\nThey are public, so they have to hide anything on an unpublished post —\nwhich means the export would silently omit the draft you have not\nfinished and the conversation under it, and an export with a silent\nomission is worse than no export. Asking as yourself makes the visibility\nquestion disappear: it is all yours, so you may all have it.\n\nAnd an export arrives whole or it is not an export. Paging it means a\nclient that stops halfway hands somebody a file that looks complete and\nis not. There is no page size here for the same reason there is no\n``fields`` parameter: the answer to \"give me my data\" is all of it.\n\n── what that costs ────────────────────────────────────────────────────\nAn unbounded response, which on a feed this size is a few hundred\nkilobytes at the very worst. If that ever stops being true the fix is a\njob that builds the file and a link that fetches it, not a page\nparameter — the shape of the promise does not change.",
        "operationId": "export_me_api_v1_users_me_export_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "account": {
                    "created_at": "2026-08-21T09:14:02.511Z",
                    "id": 7,
                    "username": "marenholt"
                  },
                  "comments": [
                    {
                      "content": "I walked past it a hundred times.",
                      "created_at": "2026-09-19T08:14:00Z",
                      "id": 12,
                      "post_id": 3,
                      "post_title": "The bus timetable is a work of fiction"
                    }
                  ],
                  "email": "ada@example.com",
                  "exported_at": "2026-09-21T10:00:00Z",
                  "posts": [
                    {
                      "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                      "created_at": "2026-09-08T22:41:07.220Z",
                      "id": 12,
                      "published": true,
                      "saved": false,
                      "title": "The library that stays open all night",
                      "updated_at": "2026-09-08T22:41:07.220Z",
                      "user": {
                        "created_at": "2026-08-21T09:14:02.511Z",
                        "id": 7,
                        "username": "marenholt"
                      },
                      "user_id": 7,
                      "voted": false,
                      "votes": 4
                    }
                  ]
                },
                "schema": {
                  "$ref": "#/components/schemas/Export"
                }
              }
            },
            "description": "Successful Response"
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "Rate limited. The message names the limit you hit."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Take your writing with you",
        "tags": [
          "Users"
        ]
      }
    },
    "/api/v1/users/{username}": {
      "get": {
        "description": "One person, by username. Case-insensitive, because names are stored\nfolded and nobody types their own capitals the same way twice.",
        "operationId": "get_user_api_v1_users__username__get",
        "parameters": [
          {
            "in": "path",
            "name": "username",
            "required": true,
            "schema": {
              "title": "Username",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "created_at": "2026-08-21T09:14:02.511Z",
                  "id": 7,
                  "username": "marenholt"
                },
                "schema": {
                  "$ref": "#/components/schemas/UserOut"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "A public profile",
        "tags": [
          "Users"
        ]
      }
    },
    "/api/v1/users/{username}/posts": {
      "get": {
        "description": "Everything by one person — same page shape as the feed, same rules.\n\nIncluding the draft rule: their unpublished posts are theirs, so this shows\nthem to the author and to nobody else.",
        "operationId": "get_user_posts_api_v1_users__username__posts_get",
        "parameters": [
          {
            "in": "path",
            "name": "username",
            "required": true,
            "schema": {
              "title": "Username",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "page",
            "required": false,
            "schema": {
              "default": 1,
              "minimum": 1,
              "title": "Page",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "page_size",
            "required": false,
            "schema": {
              "default": 10,
              "maximum": 100,
              "minimum": 1,
              "title": "Page Size",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "has_next": false,
                  "has_prev": false,
                  "items": [
                    {
                      "content": "There's a reading room near the old tram depot that never closes. No membership, no desk, no one checking whether you belong there.",
                      "created_at": "2026-09-08T22:41:07.220Z",
                      "id": 12,
                      "published": true,
                      "saved": false,
                      "title": "The library that stays open all night",
                      "updated_at": "2026-09-08T22:41:07.220Z",
                      "user": {
                        "created_at": "2026-08-21T09:14:02.511Z",
                        "id": 7,
                        "username": "marenholt"
                      },
                      "user_id": 7,
                      "voted": false,
                      "votes": 4
                    }
                  ],
                  "page": 1,
                  "page_size": 10,
                  "pages": 1,
                  "total": 3
                },
                "schema": {
                  "$ref": "#/components/schemas/PostPage"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such row — or one you aren't allowed to know about. Somebody else's unpublished post answers this, deliberately, because a 403 would confirm that it exists."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "summary": "Everything by one person",
        "tags": [
          "Users"
        ]
      }
    },
    "/api/v1/vote/": {
      "post": {
        "description": "Add or remove your upvote.\n\nOne vote per person per post, enforced by the composite primary key on\n`votes` rather than by the check below — which is what makes two requests\nracing each other a 409 instead of a 500.\n\nYou can't vote on a draft you aren't allowed to see, and that is reported\nas missing rather than forbidden, exactly as reading it would be.",
        "operationId": "vote_api_v1_vote__post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/Vote"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "example": {
                  "message": "Successfully added vote"
                },
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Vote Api V1 Vote  Post",
                  "type": "object"
                }
              }
            },
            "description": "Recorded. `dir: 0` answers 201 too, with `successfully deleted vote`."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No access token, or one that has expired, been tampered with, or belongs to a deleted account. Trade the refresh cookie for a new token at `POST /auth/refresh` rather than sending the person back to a sign-in form."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "No such post — or, with `dir: 0`, no vote of yours to remove. A client rolling back an optimistic upvote can treat this as success: it means you are already in the state you asked for."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Detail"
                }
              }
            },
            "description": "You have already voted on this post. Same advice as the 404: the state you asked for is the state you're in."
          },
          "422": {
            "description": "The body or the query string didn't validate. FastAPI's own shape: `detail` is a list of `{loc, msg, type}`, one per field."
          }
        },
        "security": [
          {
            "OAuth2PasswordBearer": []
          }
        ],
        "summary": "Upvote a post, or take it back",
        "tags": [
          "Vote"
        ]
      }
    },
    "/healthz": {
      "get": {
        "description": "Answers as long as the process is serving.\n\nDeliberately does *not* touch the database: a health check that fails when\nPostgres is slow turns one degraded dependency into a restart loop.",
        "operationId": "healthz_healthz_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "example": {
                  "status": "ok"
                },
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Healthz Healthz Get",
                  "type": "object"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Liveness probe",
        "tags": [
          "Health"
        ]
      }
    }
  },
  "servers": [
    {
      "description": "A local development server",
      "url": "http://localhost:8000"
    }
  ],
  "tags": [
    {
      "description": "Two tokens, on purpose. A short-lived **access token** comes back in the response body and goes out as `Authorization: Bearer …`; a long-lived **refresh token** goes out in an httpOnly cookie scoped to `/auth`, where no script on the page can read it and no content route ever sees it. `POST /auth/refresh` trades one for the other and rotates both.\n\n`/login` takes an **email**, not a username — the form field is called `username` because the OAuth2 password flow names it that.",
      "name": "Authentication"
    },
    {
      "description": "The feed and everything on it. Reading is public; writing needs a token. One rule runs through all of it: an unpublished post is visible to its author and to nobody else, and to anyone else it answers **404** rather than 403 — a 403 would confirm it exists.",
      "name": "Posts"
    },
    {
      "description": "Remarks on a post, oldest first, in the same page envelope the feed uses. A comment is yours to remove and nobody else's — not even the author of the post it sits on.",
      "name": "Comments"
    },
    {
      "description": "Accounts and public identities. The **username** is what everyone sees; the **email** is a credential and leaves the server on exactly one endpoint, `GET /users/me`, where the caller is the only person it belongs to.",
      "name": "Users"
    },
    {
      "description": "Posts saved to read later. A shelf is a **set**, so saving is idempotent — `PUT` and `DELETE` both answer 204 whether or not anything changed, and a client never has to treat one of its own errors as success.",
      "name": "Shelf"
    },
    {
      "description": "One upvote per person per post. There is no downvote — the only live quantity in this product is agreement.",
      "name": "Vote"
    },
    {
      "description": "Is the process up and serving.",
      "name": "Health"
    }
  ]
}
