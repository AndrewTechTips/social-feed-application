// The one place the backend lives. Change this if the API moves.
// The FastAPI dev server allows CORS from http://localhost:5173 — serve the
// frontend there (see README). Another port means adding that exact origin to
// backend/app/main.py.
export const API_BASE = "http://localhost:8000";
